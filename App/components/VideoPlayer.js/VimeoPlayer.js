
import React from 'react';
/*
import Video from 'react-native-video';

<Video source={{ uri: value.eotwVideoURL }}
            ref={(ref) => { this.player = ref }}                                      // Store reference
            onBuffer={this.onBuffer}                // Callback when remote video is buffering
            onError={this.videoError}               // Callback when video cannot be loaded
            style={styles.video}
            controls={true}
          />

      */

export default VimeoPlayer = (props) =>{
  return <Text>STUB: Imagine a video playing, it is the bestest video in the whole world.  Yep, I need to wait for that video to exist first, come back them.</Text>
}